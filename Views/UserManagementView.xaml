<UserControl x:Class="StudentManagementSystem.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Add User" Margin="5" Click="AddUser_Click"/>
            <Button Content="Edit User" Margin="5" Click="EditUser_Click"/>
            <Button Content="Delete User" Margin="5" Click="DeleteUser_Click"/>
        </StackPanel>
        <DataGrid x:Name="UsersGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Student ID" Binding="{Binding IdStudent}" Width="100"/>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="150"/>
                <DataGridTextColumn Header="Password" Binding="{Binding Password}" Width="150"/>
                <DataGridTextColumn Header="Note" Binding="{Binding Note}" Width="200"/>
                <DataGridCheckBoxColumn Header="Status" Binding="{Binding Status}" Width="80"/>
                <DataGridTextColumn Header="Created At" Binding="{Binding CreatedAt, StringFormat=dd/MM/yyyy}" Width="120"/>
                <DataGridTextColumn Header="Modified At" Binding="{Binding ModifiedAt, StringFormat=dd/MM/yyyy}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>