<UserControl x:Class="StudentManagementSystem.Views.SearchGradesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Search Student Grades" FontSize="16" Margin="0,0,0,10"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Student ID:" Margin="0,0,10,0" VerticalAlignment="Center"/>
            <TextBox x:Name="txtStudentId" Width="200" Margin="0,0,10,0"/>
            <Button Content="Search" Width="80" Click="Search_Click"/>
        </StackPanel>
        <DataGrid x:Name="GradesGrid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Student Name" Binding="{Binding StudentName}" Width="200"/>
                <DataGridTextColumn Header="Subject Name" Binding="{Binding SubjectName}" Width="200"/>
                <DataGridTextColumn Header="Mark" Binding="{Binding Mark}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>