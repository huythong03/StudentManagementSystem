<Window x:Class="StudentManagementSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="300" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Student Management System" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,20"/>
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Username:"/>
            <TextBox x:Name="txtUsername" Width="200"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Password:"/>
            <PasswordBox x:Name="txtPassword" Width="200"/>
        </StackPanel>
        <Button Grid.Row="3" Content="Login" Width="100" Click="Login_Click" HorizontalAlignment="Center"/>
    </Grid>
</Window>