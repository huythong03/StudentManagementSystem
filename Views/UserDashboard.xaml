<Window x:Class="StudentManagementSystem.Views.UserDashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="User Dashboard" Height="600" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Background="#F0F0F0">
            <Button Content="Enroll Subject" Margin="10" Click="EnrollSubject_Click"/>
            <Button Content="View Enrolled Subjects" Margin="10" Click="ViewEnrolledSubjects_Click"/>
            <Button Content="Update Account" Margin="10" Click="UpdateAccount_Click"/>
            <Button Content="Logout" Margin="10" Click="Logout_Click"/>
        </StackPanel>
        <ContentControl x:Name="MainContent" Grid.Column="1" Margin="10"/>
    </Grid>
</Window>