<UserControl x:Class="StudentManagementSystem.Views.UserRoleManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Add User Role" Margin="5" Click="AddUserRole_Click"/>
            <Button Content="Edit User Role" Margin="5" Click="EditUserRole_Click"/>
            <Button Content="Delete User Role" Margin="5" Click="DeleteUserRole_Click"/>
        </StackPanel>
        <DataGrid x:Name="UserRolesGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="100"/>
                <DataGridTextColumn Header="Student ID" Binding="{Binding IdStudent}" Width="150"/>
                <DataGridTextColumn Header="Role ID" Binding="{Binding IdRole}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>