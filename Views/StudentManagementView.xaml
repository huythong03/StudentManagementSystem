<UserControl x:Class="StudentManagementSystem.Views.StudentManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Add Student" Margin="5" Click="AddStudent_Click"/>
            <Button Content="Edit Student" Margin="5" Click="EditStudent_Click"/>
            <Button Content="Delete Student" Margin="5" Click="DeleteStudent_Click"/>
        </StackPanel>
        <DataGrid x:Name="StudentsGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="100"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Date of Birth" Binding="{Binding BOF, StringFormat=dd/MM/yyyy}" Width="120"/>
                <DataGridTextColumn Header="Province ID" Binding="{Binding IdProvince}" Width="100"/>
                <DataGridCheckBoxColumn Header="Gender" Binding="{Binding Gender}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>